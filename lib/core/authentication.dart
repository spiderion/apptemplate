//import 'package:firebase_auth/firebase_auth.dart';

/*class Authentication {
  final FirebaseAuth _auth;

  Authentication(this._auth);

  Future<String> getUserUID() async => (await _auth.currentUser())?.uid;

  Future<AuthResult> signInWithEmailAndPassword({String email, String password}) {
    return _auth.signInWithEmailAndPassword(email: email, password: password);
  }

  Future<void> sendPasswordResetEmail({String email}) {
    return _auth.sendPasswordResetEmail(email: email);
  }

  signOut() {
    _auth.signOut();
  }

  fetchSignInMethodsForEmail({String email}) {
    return _auth.fetchSignInMethodsForEmail(email: email);
  }

  Future<AuthResult> createUserWithEmailAndPassword({String email, password}) {
    return _auth.createUserWithEmailAndPassword(email: email, password: password);
  }
}*/
